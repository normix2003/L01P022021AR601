@model IEnumerable<L01P022021AR601.Models.facultades>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.acultad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.acultad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.acultad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
